from setuptools import setup, find_packages

setup(
    name='simple-word2vec',
    version='0.0.0',
    packages=find_packages(),
    author='Louis Yang',
    install_requires=['numpy', 'pandas', 'scipy'],
)
